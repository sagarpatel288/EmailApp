<resources>

    <!-- Base application theme. -->
    <style name="AppThemeGmail" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <item name="colorPrimary">@color/colorRed</item>
        <item name="colorPrimaryDark">@color/colorRedDark</item>
        <item name="colorAccent">@color/colorRed</item>
        <item name="colorButtonNormal">@color/colorRed</item>
    </style>

    <style name="Gmail" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorRed</item>
    </style>

    <!-- Base application launcher theme. -->
    <style name="AppThemeGmail.Launcher">
        <item name="colorPrimaryDark">@color/colorRed</item>
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>

    <!-- Toolbar theme -->
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:textColorHint">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <item name="actionOverflowButtonStyle">@android:color/white</item>
        <item name="drawerArrowStyle">@android:color/white</item>
    </style>

</resources>
